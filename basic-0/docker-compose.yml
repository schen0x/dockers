version: '3.9'
services:
    web:
        build: .
        ports: 5000:8080
        # for live code-update.
        volumes:
            - ./web/src:/usr/src/

    db:
#        image: "mysql"
#        environment:
#            MYSQL_ROOT_PASSWORD: password
#        volumes:
#            - db-data:/foo 

volumes:
    db-data:
